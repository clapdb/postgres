Project agent guidance — including the branching/PR workflow (master tracks
upstream; develop on `pagestore`; cherry-pick to `branchdb_N` release branches;
PRs base = `pagestore`) — is in @AGENTS.md.
